// Only Global variables that don't belong in colors, fonts, spacing ect go here.

import {
  Colors,
} from '.'

// BORDER-RADIUSES
export const globalRadius = 3
export const globalRounded = 100

// BORDERS
export const borderRight = {
  borderRightWidth: 1,
  borderStyle: 'solid',
  borderColor: Colors.dividerColor,
}
